
const Bookmarknotification = ({blog_title, onClose}) => {
    return (
        <div className="mt-5 border-red-300 p-3 mb-4">
           <h2>Bookmark added for{blog_title}</h2> 
          
        </div>
    );
};

export default Bookmarknotification;